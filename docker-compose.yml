services:
  web:
    build:
      dockerfile: Dockerfile
      secrets:
        - env
      args:
        REPO: https://github.com/formal-methods-mpi/MinervasArchive
        BRANCH: main
    env_file:
      - .env
    ports:
      - "8501:8501"
secrets:
  env:
    file: ./.env
